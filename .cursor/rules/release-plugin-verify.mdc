---
description: nxt DWC plugin releases — build gate, manual plugin load, version/manifest alignment
alwaysApply: true
---

# nxt plugin releases and versioning

## Manifest alignment

- **`ui/plugin.json`:** `dwcVersion` is **`auto`** (resolved to the **exact** DWC `package.json` version at build, e.g. `3.7.0-beta.1`). `rrfVersion` stays **`auto-major`**. Pin in [`ci/dwc-build-ref`](ci/dwc-build-ref); run `./dist/verify-dwc-build-alignment.sh <DWC>` before every `build-plugin.sh`.
- **Branch ↔ RRF/DWC:** nxt **`v0.M.0`** release branches target RRF/DWC **`3.M.x`** (e.g. branch `v0.7.0` → **3.7.x**). See [`docs/VERSIONING.md`](docs/VERSIONING.md).
- **When changing the dev/reference stack:** Update [`docs/RRF_REFERENCE.md`](docs/RRF_REFERENCE.md), [`README.md`](README.md), [`ui/src/index.ts`](ui/src/index.ts), and [`docs/UI_DEVELOPMENT.md`](docs/UI_DEVELOPMENT.md) as needed. [`docs/FEATURES.md`](docs/FEATURES.md) back-compat line tracks resolved `plugin.json` versions in shipped ZIPs.

## Gate before release commits or version tags

1. Run `./dist/audit-naming.sh` and `node dist/verify-nxt-plugin-contract.mjs` from the **MOS-nxt repo root**.
2. Run `./dist/check-macro-line-length.sh` from the **MOS-nxt repo root** (all `macros/**/*.g` lines ≤255 characters — RRF G-code line limit).
3. Run `./dist/check-line-endings.sh` from the **MOS-nxt repo root** (no CRLF in tracked text files).
4. If **`macros/nxt-config/`**, **manifest generator**, or **UI path helpers** changed: run `node dist/generate-nxt-config-manifest.mjs` and follow [plugin-ui-paths.mdc](plugin-ui-paths.mdc) (grep `nxt/config`, confirm `platforms[].machineEntryPath`, ZIP contains `dwc/js/nxt.*.js`).
5. Run `./dist/verify-dwc-build-alignment.sh <path-to-DuetWebControl>` then `./dist/build-plugin.sh <same-DWC>` from the **MOS-nxt repo root**; confirm **exit code 0**. The shipped ZIP’s `plugin.json` `dwcVersion` must match the printer’s DWC version (e.g. `3.7.0-beta.1`, not merely “3.7.x”). If the host is a different patch, rebuild the plugin against that DWC tree. **UI changes must pass the DWC vue-tsc gate** — do not ship with `NXT_SKIP_DWC_TYPECHECK=1`.
6. **Manual verification:** Do not commit or tag a release until the user has **explicitly confirmed** the plugin **loads and works** in DWC on the target line (install ZIP, start plugin, smoke-test). Treat CI success as necessary but **not sufficient**.

## Tagging conventions (this repo)

- **Betas:** Increment **`vMAJOR.MINOR.PATCH-beta.N`** (e.g. next after `v0.6.0-beta.3` is `beta.4`). Use **`git tag -a`** with a short message.
- **Release candidates:** **`vMAJOR.MINOR.PATCH-rcN`** (e.g. `v0.6.0-rc1`, `v0.6.0-rc2` — no dot before the rc number). Same CI release workflow as beta/stable tags.
- **Stable:** Avoid **`git tag -f`** on an existing stable tag unless the user explicitly asks; prefer a new semver tag.

## CI

- [`.github/workflows/release.yml`](.github/workflows/release.yml) checks out **this repo only**; DWC is fetched as a **read-only GitHub archive** via [`dist/ci-fetch-dwc.sh`](dist/ci-fetch-dwc.sh) (pin: [`ci/dwc-build-ref`](ci/dwc-build-ref)). CI does **not** `actions/checkout` or push to Duet3D/DuetWebControl or any other external repository.
- Local `build-plugin.sh` + user sign-off remain the norm before tagging.
